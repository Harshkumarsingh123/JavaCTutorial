# JavaC Tutorial

A beginner-friendly tutorial project built using modern web tools to teach **Core Java concepts** interactively.

> ⚠️ Note: This repository currently consists of frontend code (HTML/CSS/JS) — it appears to be a UI for Java tutorials, not pure Java source code.

## 📌 Overview

This project provides an interactive tutorial interface for learning **Java concepts**. It includes:

- A web-based interface built with **HTML, JavaScript, and CSS**  
- Project configured using **Vite** (fast frontend tooling)  
- Source files located in the `src/` directory  
- A tutorial homepage in `index.html`

## 🚀 Features

✅ Simple and clean tutorial layout  
✅ Ready for adding Core Java lessons  
✅ Easy to extend with additional topics  
✅ Uses modern JavaScript tooling (Node/Vite)

## 🗂️ Project Structure
JavaCTutorial/ ├── src/                  # Source code (JS, components) ├── index.html            # Main UI page for the tutorial ├── package.json          # Project metadata / dependencies ├── vite.config.js        # Vite configuration for dev server & build ├── .gitignore            # Files to ignore in Git

## 🛠️ Setup & Installation

Make sure you have **Node.js & npm** installed.

1. **Clone the repository**
   ```bash
   git clone https://github.com/Harshkumarsingh123/JavaCTutorial.git
   cd JavaCTutorial
